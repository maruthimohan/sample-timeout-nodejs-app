{
  "name": "timeout-app",
  "version": "1.0.0",
  "description": "This app is used for mimicking a timeout in Salesforce",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maruthi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "engines": {
    "node": "14.x"
  }
}
